# Python >= 3.10 required (MCP SDK dependency)
pyautogui>=0.9.53
psutil>=5.9.0
mcp[cli]>=1.0.0
pystray>=0.19.5
Pillow>=9.0.0

# Windows 依赖
pywin32>=307; sys_platform == 'win32'

# macOS 依赖
pyobjc-core>=10.0; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=10.0; sys_platform == 'darwin'
pyobjc-framework-Quartz>=10.0; sys_platform == 'darwin'

# Linux 系统依赖（需手动安装）：
#   sudo apt install xdotool wmctrl xclip
# Linux Python 依赖（如果 xclip 不可用，可用 pyperclip 代替）
pyperclip>=1.8.2; sys_platform == 'linux'
